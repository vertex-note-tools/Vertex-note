{
  "name": "gemini-vertex-backend",
  "version": "1.0.0",
  "main": "index.js",
    "scripts": {
     start": "functions-framework --target=geminiVertexNote --port=${PORT:-8080}"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "google-auth-library": "^9.0.0"
  }
}
